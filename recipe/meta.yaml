{% set name = "PyAthena" %}
{% set version = "1.7.2" %}

package:
  name: "{{ name|lower }}"
  version: "{{ version }}"

source:
  url: https://pypi.io/packages/source/{{ name[0] }}/{{ name }}/{{ name }}-{{ version }}.tar.gz
  sha256: b0a30a3d4ba276d2d3e2565a9e38205caa0dcd3a211e5df09f3b92086144c0f4

build:
  number: 0
  script: "{{ PYTHON }} -m pip install . --no-deps --ignore-installed -vv "

requirements:
  host:
    - python
    - pip
  run:
    - boto3 >=1.4.4
    - botocore >=1.5.52
    - future
    - futures  # [py27]
    - python
    - setuptools
    - tenacity >=4.1.0

test:
  imports:
    - pyathena
    - tests
  requires:
    - pytest >=3.5
    - pytest-cov
    - pytest-flake8 >=1.0.1
    - sqlalchemy >=1.0.0,<1.3.0

about:
  home: https://github.com/laughingman7743/PyAthena/
  license: MIT
  license_family: MIT
  license_file: LICENSE
  summary: Python DB API 2.0 (PEP 249) compliant client for Amazon Athena
  dev_url: https://github.com/laughingman7743/PyAthena/

extra:
  recipe-maintainers:
    - JorgeGarciaIrazabal
    - mariusvniekerk
